# tasks2 — Iterated Task Manager

**What’s new vs tasks1**
- Task fields: `id`, `title`, `description`, `due (YYYY-MM-DD)`, `priority (low/medium/high)`, `tags`, `completed`
- Filters: overdue, due today, open, done, by priority, by tag
- Mark complete
- Data file: `data/tasks2.json` (auto-created)

## Run
```bash
cd csc299-project
python tasks2/cli.py

```